# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg
dist/
build/
.eggs/
*.so

# Virtual environment
venv/
.venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Testing
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage

# Data — sensitive / large (DO NOT commit)
data/
data_real/
data_backup/
data_*/
logs/
chroma_data/
# 不带斜杠：`backups/` 只匹配真目录。真实部署常把它做成软链指向仓外的
# 持久备份库（备份不该跟着仓库走），而软链不是目录 —— 规则当场失效，
# 备份入口就这样露在 git status 里等着被误提交。
backups
.deps/
exports/

# Secrets & config (local only)
.sf_key
.llm_key
.opencode_zen_key
.sensenova_key
mem0_config_local.json

# Legacy archives
*.bak*
*.old*
legacy/archive/

# OS
.DS_Store
Thumbs.db

# Misc
.env
*.log
.sensenova_key
.local/

uv.lock
