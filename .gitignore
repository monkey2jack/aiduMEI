# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg
dist/
build/
.eggs/
*.so

# Virtual environment
venv/
.venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Testing
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage

# Data — sensitive / large (DO NOT commit)
data/
data_real/
data_backup/
data_*/
logs/
chroma_data/
# 不带斜杠：`backups/` 只匹配真目录。真实部署常把它做成软链指向仓外的
# 持久备份库（备份不该跟着仓库走），而软链不是目录 —— 规则当场失效，
# 备份入口就这样露在 git status 里等着被误提交。
backups
.deps/
exports/

# Secrets & config (local only)
.sf_key
.llm_key
.opencode_zen_key
.sensenova_key
mem0_config_local.json

# Legacy archives
*.bak*
*.old*
legacy/archive/

# OS
.DS_Store
Thumbs.db

# Misc
.env
*.log
.sensenova_key
.local/

uv.lock

# 评测运行留证（本地产物，随每次运行变化，不进仓库；manifest 除外——它是承诺）
benchmarks/runs/

# ── 秘密面兜底（v20.0pre 补：`.env` 一条盖不住 `.env.prod` 这种人手起的名字）──
# `.env` 已在上面单列；这里把所有 `.env.*` 变体一网打尽，再把仓里必须保留的
# 示例文件负向豁免回来。负向规则必须写在广谱规则之后，否则不生效。
.env.*
!.env.example

# 私钥 / 证书 / 口令文件：这类东西一旦误提交，改历史都救不回来（远端已抓走）。
# 当前仓里零命中，所以这几条是纯净的兜底，不会误伤任何在跟踪的文件。
*.pem
*.key
*.p12
id_rsa*
id_ed25519*
secrets.json

# 脱敏词表：铁律 0 说「词表一律外置于仓库之外 —— 词表里装的正是要藏的东西，
# 写进仓库等于为了防泄露而制造一次泄露」。这条把那句话从「记得住」变成「进不来」。
scan_words.txt
*.scanwords

# SQLite 库：默认落点是 `data/`（已忽略），但 AIDUMEM_DATA_DIR 一改就可能落到仓里。
*.db
*.sqlite
*.sqlite3

# macOS 扩展属性伴生文件（AppleDouble）。
# v20：本机 50 个文件带 xattr，`tar` 默认会为它们生成 `._xxx` 伴生条目 ——
# 打包时用 COPYFILE_DISABLE=1 挡掉；这一条挡的是「哪天有人手滑 git add 进来」。
._*
.DS_Store
docs/README_draft.md
