# aiduMEI Testing

## 当前结果与复现环境

当前收集数、各环境的通过/跳过数与复现命令统一记录在 [README 测试与质量](../README.md#测试与质量) 和 [English testing section](../README_EN.md#testing--quality)。本页保留测量规则和历史；不再维护另一张易过期的当前数字表。

每条测量记录必须包含版本或提交、日期、Python/依赖环境、模型缓存状态、命令及最终退出码。通过、失败、错误和跳过分别记录；有失败的执行不能称为全量通过。仅执行 `pytest --collect-only` 能证明收集数，不能证明这些用例通过。

“完整 extras”还需要本地嵌入模型缓存，安装 `fastembed` 本身并不代表模型就绪。`AIDUMEI_LOCAL_EMBED_CACHE` 指定测试缓存，默认是 `~/.cache/aidumei/fastembed`；使用 `scripts/fetch_local_embed_model.py` 在部署期取模，运行时保持离线。只缺 Hermes 宿主时应跳过宿主用例；同时缺模型缓存则还会跳过真模型用例，必须作为另一个环境记录。

```bash
# 完整可选环境；先部署模型再测试
pip install -r requirements.txt -r requirements-dev.txt
pip install "mcp>=1.0.0,<2" ruff nltk regex numpy fastembed
python scripts/fetch_local_embed_model.py
HERMES_SRC=none pytest tests/ -q -rs
# 宿主在场；其余工具、依赖、数据集与缓存也必须齐备
HERMES_SRC=/path/to/hermes-agent pytest tests/ -q -rs
```

公开 LoCoMo 数据集也必须单独就位；按 [评测协议](../benchmarks/PROTOCOL.md) 获取 `locomo10.json`，并把 `AIDUMEI_BENCH_DATA_DIR` 指向其目录。测试树不携带数据本体；隔离 HOME 后尤其不能假定默认路径仍能找到原数据。

2026-09-07 的 v20.3.4 远程两形态基于源码 `877310a`，均使用隔离 HOME、不带 `.env`，显式指定模型缓存和公开基准数据目录；全轴 venv 与生产 venv 分别测量，最终进程均退出 0。具体数字见 README 当前表。

基础安装测量必须另建干净 venv，仅安装 `requirements.txt` 和 `requirements-dev.txt`，不能在完整环境里重新执行安装命令便称为“基础路径”。模型路径和宿主探测状态也要一并记录。

新增用例后，先更新实际收集数。未复测的环境标记“待复测”，保留原日期、原总数的历史基线；按轴推导的数字必须明确称为推导值。不要把新数字配上旧测量日期，也不要把额外跳过改写成只有 Hermes 缺席的完整环境。

`tests/test_v19_4_1_audit_fixes.py` 校验 README 当前收集数、宿主轴以及中英文数字一致性；`tests/test_v20_skip_axis_census.py` 从实际门控位点核对跳过轴。历史测量与当前收集数是不同证据，守卫须分别检查。

## 历史矩阵的口径

2026-08-29 的无配置/带配置两个克隆均为 1497 通过、2 跳过，总数 1499。该对照用于证明评分测试不依赖重排服务是否可达；它不是当前版本的测试结果。更早迁入本页的 1550/1538 表已撤下，因为它未注明对应版本，且错误地把不同总数时代的行放在同一等式下。

2026-09-03 的 v20.3.2 基线为：生产机沙箱 1723 通过、5 跳过；全轴 venv 1728 通过、0 跳过。沙箱实际跳过 ruff×3 与 mcp×2。真模型用例在该全量会话中未跳过，但单独新进程对默认模型路径探测失败；因此这个历史结果不能证明默认缓存已部署，后续完整环境测量须显式提供并核验模型缓存。

2026-09-07 的文档订正撤回了把“缺宿主和模型缓存”的本机结果写进“只缺宿主的完整环境”，且沿用 2026-09-03 日期的写法。后续数字以相应环境的新一轮完整日志为准。

全量套件按 Linux/macOS（POSIX）维护，以模块单元测试、源码守卫和 TestClient 接口测试为主。真实 mem0/Qdrant、真实模型调用及恢复演练由单独的集成或生产冒烟证据支持，单元测试通过不能代替这些证据。

---

## 「全轴齐备」这一行的来历（v20.3.2 从 README 迁入）

> README 只留结论与数字，历史叙事放这里 —— 它是**踩坑记录**，不是用户装完要读的东西。

**「全轴全绿」曾被实测过**（2026-09-02 生产机，总数 1573、五轴齐备、0 跳过）。这一行的来历值得留着：
它最早在 2026-08-24 拿到实测（总数 1112），2026-08-25 复测为 1200/0（那时是九条轴）；
之后轴表长到十三条，齐备形态一度只剩推导值 —— 直到这次为它单独建了一个带 `mcp` extra 的测试 venv。

更早的一版 README 把它写成「生产实跑核验」，而它恰恰被自己引用的那次生产实跑当场证伪；
于是改成「推导值，从未实测」，并配了一条**会随环境失效**的断言把这句话钉住 ——
真有一天齐备了，那条断言就该红。它在 2026-08-24 按设计红了，那一版才换成实测值。

**绝对措辞必须经得起自己引用的那次测量**：先让它可证伪，再谈它是真的。

## 沙箱 ≠ 线上部署树：`.git` 的三种状态

⚠️ v20.0 部署前踏勘发现：线上那棵树里躺着一份陈旧的 `.git`（早年 clone 留下的，
如今靠文件拷贝更新，索引早已落后于代码），所以 git 工作区这条轴**在那里不会跳** ——
它会拿一份过期的 `git ls-files` 当基准去比对，那是另一种错，不是同一个数。
上一版把这行叫「生产部署树」，行名和它自己的括注（「生产机沙箱实测」）当场互相矛盾。
**测的是哪棵树，就只许报哪棵树。**

**v20.3.1 起沙箱形态变了**：从「白名单拷贝、无 `.git`」改成 **bundle clone、有 `.git`**
（为了让 git 相关守卫能在沙箱里跑）。于是：

| 形态 | `.git` | git 轴 | 沙箱跳过数 |
|---|---|---|---|
| 旧沙箱（≤v20.3.0） | 无 | 跳 | 含 git 那 1 条 |
| 新沙箱（≥v20.3.1） | 有 | **不跳** | 不含 git 轴 |

数字守卫的推导式在 v20.3.2 之前仍按旧形态减 git 那 1 条 —— **守卫报「README 数字写错了」，
而 README 是对的**。这是本仓第三次「守卫的世界模型落后于它守的现实」。
